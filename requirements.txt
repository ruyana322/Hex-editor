# Hex Editor Tool - Python Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies (mostly built-in)
# Python 3.8+ required

# Optional enhancements
colorama>=0.4.3  # Enhanced color support (alternative to ANSI)

# System dependencies (install via pkg in Termux):
# - python
# - ffmpeg
# - git
